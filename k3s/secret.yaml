# Secret lgtm-secrets は Infisical (il.doany.io) のフォルダ /lgtm/lgtm-secrets にあり、
# Infisical 純正 operator (bootstrap/infisical-operator) が Secret にする。
# 値は Infisical の UI で書き換えるだけ（60秒以内に同期。auto-reload 注釈のある
# Deployment は自動で入れ替わる）。
apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: lgtm-secrets
  namespace: lgtm
spec:
  hostAPI: http://infisical.infisical.svc:8080/api
  resyncInterval: 60
  authentication:
    kubernetesAuth:
      identityId: f2bc3416-f196-4626-9bf3-c11693b32cc6
      autoCreateServiceAccountToken: true
      # ESO 時代のマシンアイデンティティが許可する SA をそのまま使う
      # （bootstrap/external-secrets/sa.yaml に経緯）
      serviceAccountRef:
        name: external-secrets
        namespace: external-secrets
      secretsScope:
        projectSlug: k3s-cluster
        envSlug: prod
        secretsPath: /lgtm/lgtm-secrets
        recursive: false
  managedKubeSecretReferences:
    - secretName: lgtm-secrets
      secretNamespace: lgtm
      # Orphan: CR を消しても Secret は残す（移行や再作成で稼働中の Pod を巻き込まない）
      creationPolicy: Orphan
